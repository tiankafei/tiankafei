package ${cfg.name.get(${table.name}).pageParamPath};

#if(${swagger2})
import io.swagger.annotations.ApiModel;
#end
#if(${entityLombokModel})
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.experimental.Accessors;
#end

/**
 * <pre>
 * $!{table.comment} 分页参数对象
 * </pre>
 *
 * @author ${author}
 * @date ${date}
 */
#if(${entityLombokModel})
@Data
@Accessors(chain = true)
@EqualsAndHashCode(callSuper = true)
#end
#if(${swagger2})
@ApiModel(value = "$!{table.comment}分页参数")
#end
public class ${cfg.name.get(${table.name}).pageParamName} extends ${cfg.name.get(${table.name}).superPageParamClass} {

    private static final long serialVersionUID = 1L;


}
