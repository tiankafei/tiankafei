<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.tiankafei.user.mapper.SysUserRoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="baseResultMap" type="org.tiankafei.user.vo.SysUserRoleQueryVo">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="role_id" property="roleId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_user_id" property="createUserId"/>
        <result column="update_user_id" property="updateUserId"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="base_Column_List">
        id, user_id, role_id, status, create_time, update_time, create_user_id, update_user_id
    </sql>

    <!-- 根据主键获取 用户拥有的角色关系表 对象 -->
    <select id="getSysUserRoleById" resultMap="baseResultMap">
        select
        <include refid="base_Column_List"/>
        from sys_user_role where id = #{id}
    </select>

    <!-- 分页查询 用户拥有的角色关系表 对象集合 -->
    <select id="getSysUserRolePageList" resultMap="baseResultMap">
        select
        <include refid="base_Column_List"/>
        from sys_user_role
    </select>

    <!-- 查询 用户拥有的角色关系表 对象集合 -->
    <select id="getSysUserRoleList" resultMap="baseResultMap">
        select
        <include refid="base_Column_List"/>
        from sys_user_role
    </select>

</mapper>
