server:
  port: 9000
spring:
  profiles:
    active: dev, dev-eureka
  application:
    name: monitor-admin-service
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  security:
    user:
      name: "admin"
      password: "admin123"
logging:
  level:
    ROOT: info
    # nacos日志的包路径
    com.alibaba.nacos.client.naming: WARN
    # nacos日志的包路径
    com.alibaba.nacos.client.config.impl: WARN
    # spring自动装配日志的包路径
    org.springframework.boot.autoconfigure: ERROR