server:
  port: 7010
spring:
  application:
    name: zuul-service
zuul:
  #全部服务忽略敏感头（全部服务可传递cookie）
  sensitiveHeaders:
  prefix: /api
  routes:
    user:
      path: /user/**
      serviceId: user-service